import { ReactNode } from 'react';
interface ContainerProps {
    children: ReactNode;
}
declare const Container: ({ children }: ContainerProps) => import("react/jsx-runtime").JSX.Element;
export default Container;
