interface ButtonProps {
    buttonHandler: () => void;
    buttonText: string;
}
declare const Button: ({ buttonHandler, buttonText }: ButtonProps) => import("react/jsx-runtime").JSX.Element;
export default Button;
